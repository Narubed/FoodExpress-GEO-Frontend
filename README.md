# Partner-FoodExpress-Frontend

install nextjs with ubuntu
- npm install / yarn
- npm run build/ yarn build
# `run start` is the command that will be executed.
- pm2 start npm --name my-project -- run start

- sudo systemctl reload nginx
